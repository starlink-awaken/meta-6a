# meta-6a Skill - 独立使用指南

> 可在任何 AI 系统中使用的 5A 核心框架 + 扩展模块 skill
>
> 版本：v0.2.1 | 打包位置：~/workspace/skills/meta-6a/

---

## 📋 概述

**meta-6a** 是一个轻量级的 AI skill，提供 5A 核心框架和可扩展模块的结构化思维验证能力。

**核心特性**：
- ✅ 轻量级：~170 行 markdown，零代码依赖
- ✅ 快速：验证 < 10秒，分析 < 60秒
- ✅ 易用：自然语言触发，无需学习
- ✅ 灵活：默认简洁，可选深入
- ✅ 可扩展：支持领域特定扩展模块

**5A 核心框架**：
1. **AIAnalyze** - 分析现状、识别问题、明确边界
2. **AIArchitect** - 设计方案、规划架构、定义接口
3. **AIBuild** - 构建产物、编写代码、生成文档
4. **AIApply** - 应用部署、测试验证、验证功能
5. **AIImprove** - 持续优化、迭代改进、收集反馈

**扩展模块**：
- **AIAgent 扩展**（AI Agent 项目专用）：在 AIArchitect 和 AIBuild 之间插入

---

## 🚀 快速开始

### 在 Claude Code 中使用

**方式1：直接调用（推荐）**

```
# 快速验证模式
验证6A "开发一个用户登录功能"
检查6A "开发一个用户登录功能"
6A检查 "开发一个用户登录功能"
meta-check "开发一个用户登录功能"
验证一下 "开发一个用户登录功能"

# 完整分析模式
完整6A分析 "开发一个用户登录功能"
deep-analysis "开发一个用户登录功能"
完整分析 "开发一个用户登录功能"
深度分析 "开发一个用户登录功能"

# 框架说明模式
6A框架
6A说明
什么是6A
6A是什么
```

**方式2：使用 Skill 工具**

```python
from claude_skills import Skill

skill = Skill.load('meta-6a')
result = skill.execute("验证6A", "开发一个用户登录功能")
```

**方式3：作为 prompt 模板**

```python
prompt = """
You are using the meta-6a framework for structured thinking.

Task: {user_input}

Follow the 6A framework:
1. AIAnalyze: ...
2. AIArchitect: ...
...
"""

AI.generate(prompt)
```

### 在其他 AI 系统中集成

#### ChatGPT / GPT-4

```markdown
## System Prompt

You are equipped with the meta-6a thinking framework.

When user requests verification or analysis, apply the 5A core framework:

1. **AIAnalyze** - Quickly scan: core problem? complete? boundaries?
2. **AIArchitect** - Assess: feasible? risks? complexity?
3. **AIBuild** - (if needed)
4. **AIApply** - (if needed)
5. **AIImprove** - (if needed)

Extension modules (load as needed):
- **AIAgent** - For AI Agent projects (insert between AIArchitect and AIBuild)

Output format: Quick Check (default) or Full Analysis (on request)
```

#### Claude / Anthropic

```xml
<!-- system prompt -->
You have access to the meta-6a skill for structured thinking.

When the user says "验证6A", "检查6A", "meta-6a", apply the meta-6a framework:

<file>~/workspace/skills/meta-6a/Workflows/quick_check.md</file>
```

#### 自定义 AI 系统

```python
class Meta6AProcessor:
    def __init__(self):
        self.skill_path = "~/workspace/skills/meta-6a/"

    def load_skill(self):
        with open(f"{self.skill_path}/SKILL.md") as f:
            skill_config = f.read()
        # 解析 skill 配置

    def quick_check(self, user_input):
        prompt = self.load_skill()
        response = ai.generate(prompt.format(input=user_input))
        return response
```

---

## 📂 文件结构

```
meta-6a/
├── SKILL.md              # 核心：定义、路由、触发词
├── CHANGELOG.md          # 版本变更记录
├── Workflows/
│   ├── quick_check.md  # 工作流：快速验证逻辑
│   └── full_analysis.md # 工作流：完整分析逻辑
├── examples/
│   └── cases.md         # 示例：真实对话示例
└── README.md            # 说明：使用指南
```

### 文件说明

**SKILL.md**
- YAML frontmatter：name、version、description、last_updated
- 5A 核心框架介绍 + 扩展模块系统
- 核心原则
- 工作流路由表（统一的触发词定义）
- 使用示例
- Quick Reference（三种输出深度规范）

**Workflows/quick_check.md**
- 触发条件（统一的触发词列表）
- 分析过程（内部思考）
- 输出格式（简洁 < 200词）
- 质量门控

**Workflows/full_analysis.md**
- 完整的 5 个核心阶段分析模板（或 5A+扩展）
- 扩展模块决策点处理
- 详细输出格式（无词数限制）
- 与快速验证的对比说明

**examples/cases.md**
- 8+ 个真实场景示例
- 每个示例包含：
  - 场景描述
  - 用户输入
  - AI 输出
  - 价值说明

**README.md**
- skill 说明
- 使用方法（包含统一的触发词）
- 版本历史
- 自定义指南

**CHANGELOG.md**
- 版本变更记录
- 遵循 Keep a Changelog 格式

---

## 🎯 核心功能

### 1. 快速验证（默认）

**触发词**：
- `验证6A`
- `检查6A`
- `6A检查`
- `meta-check`
- `验证一下`

**输出**：
```
✅ 5A快速验证

📊 问题识别
- 核心问题：[一句话]
- 完整性：[完整/不完整]
- 紧急程度：[高/中/低]
- 影响范围：[描述]

🎯 关键建议
1. [建议1]
2. [建议2]
3. [建议3]

💡 下一步行动
[具体行动]

⚠️ �险提示（如果有）
```

**特点**：
- 简洁（< 200词）
- 聚焦关键问题
- 可执行建议

### 2. 完整分析（可选）

**触发词**：
- `完整6A`
- `6A分析`
- `deep-analysis`
- `完整分析`
- `深度分析`

**输出**：
- 完整的 5A 各阶段分析（或 5A+扩展）
- 每个阶段的详细建议
- 系统性思考框架

**特点**：
- 全面（5 个核心阶段 + 扩展模块）
- 深入（具体建议）
- 系统（结构化）

---

## 💡 使用场景示例

### 场景 1：需求验证

```
用户: 验证6A "开发一个用户评论功能"

AI 输出:
✅ 5A快速验证
📊 问题识别: 功能清晰但缺少数据模型设计
🎯 关键建议:
  1. 数据模型：嵌套结构？多级回复存储？
  2. 权限控制：谁可以评论？
  3. 审核机制：如何处理垃圾评论？
💡 下一步: 进入架构阶段
⚠️ 风险: 嵌套评论性能是难点
```

### 场景 2：代码审查

```
用户: 6A检查 "代码：getUser(id) { return db.query('SELECT * FROM users WHERE id = ?', [id]); }"

AI 输出:
✅ 5A快速验证
📊 问题识别: 功能正确但缺少防护
🎯 关键建议:
  1. 参数验证
  2. 错误处理
  3. 字段选择
💡 下一步: 优化并添加测试
⚠️ 风险: 缺少错误处理可能崩溃
```

### 场景 3：技术选型

```
用户: 检查6A "使用 Redis + MySQL + Vue3"

AI 输出:
✅ 5A快速验证
📊 问题识别: 技术栈合理但缺少架构细节
🎯 关键建议:
  1. 缓存策略设计
  2. 数据同步机制
  3. 容错方案
💡 下一步: 架构设计
⚠️ 风险: 数据一致性是难点
```

---

## 🔧 自定义和扩展

### 修改触发词

编辑 `SKILL.md` 的 Workflow Routing 表：

```markdown
## Workflow Routing

| 触发词 | 工作流 | 说明 |
|--------|--------|------|
| "验证6A" | `Workflows/quick_check.md` | 原有触发 |
| "检查一下" | `Workflows/quick_check.md` | 新触发词 |
| "deep" | `Workflows/full_analysis.md` | 新触发词 |
```

### 修改输出格式

编辑 `Workflows/quick_check.md` 的输出格式部分：

```markdown
## 输出格式

✅ [自定义标题]

📊 [自定义问题识别]
- 自定义字段...

🎯 [自定义建议]
- 自定义建议格式...

💡 [自定义下一步]
- 自定义行动项...
```

### 添加领域特定示例

编辑 `examples/cases.md`：

```markdown
## 示例 N：[领域特定场景]

**场景**: [描述]
**用户输入**: [输入]
**AI 输出**: [输出]
**价值**: [价值]
```

---

## 📊 与其他 Skills 的对比

| Skill | 类型 | 触发词 | 复杂度 |
|-------|------|--------|--------|
| **meta-6a** | 思维框架 | 验证6A、检查6A | 低 |
| **Council** | 多代理辩论 | council | 中 |
| **RedTeam** | 对抗分析 | red team | 高 |
| **FirstPrinciples** | 第一性原理 | first principles | 中 |
| **Prompting** | 元提示系统 | use fabric | 高 |

**协作使用**：

```
# 验证需求后进行多代理辩论
你: 验证6A "开发一个分布式系统"
AI: [快速验证输出]
你: Council 这个架构方案
AI: [多代理辩论分析]

# 第一性原理深度分析
你: 验证6A [需求]
AI: [快速验证]
你: FirstPrinciples [核心问题]
AI: [第一性原理分析]
```

---

## 🎓 学习资源

### 理解 5A 核心框架

1. **AIAnalyze**: 快速扫描问题、检查完整性、明确边界
2. **AIArchitect**: 评估可行性、识别风险、规划结构
3. **AIBuild**: 构建代码、编写文档、生成资源
4. **AIApply**: 部署系统、集成测试、验证功能
5. **AIImprove**: 收集反馈、迭代优化、持续改进

### 扩展模块

**AIAgent 扩展**（仅 AI Agent 项目）：
- 设计 AI 角色、定义交互、规划能力
- 在 AIArchitect 和 AIBuild 之间插入

### 实践建议

**日常使用**：
- 日常对话：快速验证（默认）
- 重要决策：完整分析（可选）

**团队协作**：
- 需求评审：验证需求完整性
- 代码审查：检查代码质量
- 技术评审：评估技术方案

**个人学习**：
- 结构化思维训练
- 问题分析能力提升
- 决策框架参考

---

## 🔍 质量保证

### Skill 文件质量

- [ ] SKILL.md 包含 YAML frontmatter
- [ ] description 包含 USE WHEN 触发条件
- [ ] 工作流路由表完整
- [ ] 有 3+ 个使用示例
- [ ] README.md 包含使用指南

### 使用效果验证

- [ ] 响应时间 < 10秒（快速验证）
- [ ] 输出简洁（< 200词）
- [ ] 建议可执行
- [ ] 用户反馈正面

---

## 🚨 常见问题

### Q: Skill 不生效？

**A**: 检查以下几点：
1. 文件路径是否正确
2. YAML 格式是否正确
3. description 是否包含 USE WHEN
4. 触发词是否匹配

### Q: 输出不符合预期？

**A**: 可能原因：
1. prompt 模板需要调整
2. 需要更具体的上下文
3. 场景超出了 skill 覆盖范围

### Q: 如何添加新功能？

**A**:
1. 在 `Workflows/` 下创建新工作流
2. 在 `SKILL.md` 中添加路由
3. 在 `examples/` 中添加示例

---

## 📞 支持和反馈

**问题反馈**：
- 在 PAI 系统中报告问题
- 在 workspace/issues 提交 issue

**改进建议**：
- 在 `examples/` 中添加新示例
- 在 `SKILL.md` 中更新文档
- 分享给团队使用经验

---

**源位置**：`~/.claude/skills/meta-6a/`
**打包位置**：`~/workspace/skills/meta-6a/`

**创建者**: PAI (Personal AI Infrastructure)
**打包日期**: 2026-02-03
**版本**: v0.2.1
**许可证**: MIT License

---

## 🎉 开始使用

```bash
# 立即体验
验证6A "你的需求/设计/代码"
```

**轻量、快速、有效 - 这就是 meta-6a skill！**
